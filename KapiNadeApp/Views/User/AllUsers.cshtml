@model IEnumerable<KapiNadeApp.Models.UserMV>

@{
    ViewBag.Title = "All Users";
}

<div class="col-md-10">

    <div class="contact-form-block">

        <h2 class="contact-title">Svi korisnici</h2>

     
        <table class="table table-hover">

            <tr>
                <th>
                    Korisničko ime
                </th>
                <th>
                    Tip korisnika
                </th>
                <th>
                    Status profila
                </th>
                <th>
                    Email
                </th>
                <th></th>
            </tr>

            @foreach (var user in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => user.Username)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => user.UserType)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => user.AccountStatus)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => user.UsernameOrEmail)
                    </td>
                    <!--<td>
                        @Html.ActionLink("Uredi", "Edit", new { id = user.UserID }, new { @class = "btn btn-primary" })
                        @Html.ActionLink("Obriši", "Delete", new { id = user.UserID }, new { @class = "btn btn-danger" })
                    </td>-->
                </tr>
            }

        </table>

    </div>
</div>
