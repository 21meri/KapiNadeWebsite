@model KapiNadeApp.Models.UserTypeMV

@{
    ViewBag.Title = "User types";
}

<h3>Jeste sigurni da želite obrisati?</h3>
<div>
    <h4>Tip korisnika</h4>
    <hr />
    <dl class="custom-dl">
        <dt>
            Tip korisnika koji brišete:
        </dt>

        <dd>
            @Html.DisplayFor(model => model.UserType)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Obriši" class="btn btn-danger" />
            @Html.ActionLink("Nazad", "AllUserTypes", null, new { @class = "btn btn-theme-invert" })
        </div>
    }
</div>
